---
title: 'Interactive SQL Showcase: DataRockie Bootcamp Concepts'
subtitle: A live demonstration of key SQL queries with a modern UI
output:
  html_document: default
  pdf_document: default
---

<style>
  .section h2, .section h3 {
    display: flex;
    align-items: center;
  }
  .section h2 img, .section h3 img {
    margin-right: 10px;
  }
</style>

# 🔥 ปลุกพลัง SQL จาก DataRockie BootCamp12 Sprint SQL 🐤💛

**นอนน้อยแต่นอนนะ!** เรียนจนร้องชีวิต ไม่มีสอนเลท มีแต่สอนแถม 555+ สอนเสร็จวันเสาร์ นัดเพิ่มวันอาทิตย์🤣

โดย **Stack** วันนี้คือ การรวมพลังระหว่าง **`R`** กับ **`SQL`** เพื่อสร้างสรรค์ผลงานที่มีความหมาย ถ้าพี่ ๆ น้อง ๆ คนไหนอยากมี **`Portfolio`** เจ๋ง ๆ แบบนี้ หลังไมค์ฟลุคได้ 555+

โดยเนื้อหาในวันนี้ฟลุคจะใช้ **`Data`** จาก **`Chinook Database`** และโครงสร้างการเรียนรู้จะแบ่งออกเป็น 2 ส่วนหลัก: **แนวคิด SQL** และ **เครื่องมือการนำเสนอ**

### 1. SQL Clause ในการดึงข้อมูล

เราจะเจาะลึกคำสั่งพื้นฐานและขั้นสูงตามลำดับการประมวลผลของ Database Engine ซึ่งเป็นหัวใจสำคัญของการเขียนคิวรี:

1.  **`FROM`** & **`JOIN`**: การระบุตารางและการรวมแหล่งข้อมูล.
2.  **`WHERE`**: การ **กรองแถว** (Rows) ตามเงื่อนไข (ก่อนการจัดกลุ่ม).
3.  **`GROUP BY`** & **`AGGREGATE FUNCTION()`**: การ **จัดกลุ่ม** และการสรุปผลข้อมูลด้วยฟังก์ชัน เช่น `COUNT()`, `SUM()`, `AVG()`.
4.  **`HAVING`**: การ **กรองกลุ่ม** ตามเงื่อนไขของผลลัพธ์ที่สรุปแล้ว.
5.  **`SELECT`**: การกำหนดคอลัมน์สุดท้ายที่จะแสดงผล.
6.  **`ORDER BY`**: การ **จัดเรียง** ผลลัพธ์สุดท้าย.
7.  **`CASE WHEN`**: ตรรกะแบบมีเงื่อนไข (If-Then-Else) สำหรับการสร้างคอลัมน์ใหม่.
8.  **`Common Table Expressions (CTEs)`**: เทคนิคขั้นสูงเพื่อจัดโครงสร้างคิวรีที่ซับซ้อนให้ดูสะอาดตา.

### 2. เครื่องมือการนำเสนอผลงาน (Portfolio Tools) 💻

เอกสารนี้ใช้พลังของ **R** เพื่อนำเสนอผลงานได้อย่างยอดเยี่ยม:

-   **โค้ด SQL ที่รันได้จริง** (Executable Code): คุณสามารถคัดลอกและนำไปใช้ต่อได้ทันที
-   **ผลลัพธ์แบบอินเทอร์แอคทีฟ** 📊: ผลลัพธ์จะแสดงในรูปแบบตารางที่สามารถ **ค้นหา** และ **จัดเรียง** ได้ด้วยแพ็คเกจ `DT`

**ลุยกัน เย้!**

------------------------------------------------------------------------

## 3 ขั้นตอนหลักในการเรียนรู้ 🚀

คุณจะได้เรียนรู้ SQL แบบลงมือทำผ่านขั้นตอนเหล่านี้:

1.  **การตั้งค่าและเชื่อมต่อฐานข้อมูล** ⚙️
    -   ติดตั้งไลบรารี **R** ที่จำเป็น (`DBI`, `RSQLite`, `DT`).
    -   **ดาวน์โหลด** และ **สร้างการเชื่อมต่อ** ไปยังฐานข้อมูลตัวอย่าง **Chinook** เพื่อให้ R สามารถส่งคำสั่ง SQL ได้.
2.  **การสำรวจและรันคำสั่ง SQL สำคัญ** 💡
    -   ทำความเข้าใจและรันคำสั่งหลัก ๆ เช่น **JOIN**, **GROUP BY**, **HAVING**, **CASE WHEN**, และ **Common Table Expressions (CTEs)**.
    -   คิวรีเหล่านี้จะช่วยให้คุณสามารถรวมตาราง กรองข้อมูล และสร้างการวิเคราะห์เชิงลึกได้.
3.  **การวิเคราะห์ผลลัพธ์แบบโต้ตอบ** 📊
    -   **ตรวจสอบ** ผลลัพธ์ของคิวรีแต่ละข้อในรูปแบบ **ตารางแบบอินเทอร์แอคทีฟ** (Interactive Table) ด้วยแพ็คเกจ **DT**.
    -   คุณสามารถ **ค้นหา** และ **เรียงลำดับ** ข้อมูลได้ทันที ซึ่งช่วยให้เห็นภาพการทำงานของคำสั่ง SQL ได้อย่างชัดเจน.

------------------------------------------------------------------------

## ![](DuckCool.png){width="22"} 1. Environment Setup

## บทนำและขั้นตอนการเรียนรู้

บทความนี้จะพาคุณไปรู้จักกับคำสั่ง SQL สำคัญ ๆ ที่ใช้ในการจัดการข้อมูล โดยใช้ฐานข้อมูลเป็นตัวอย่าง คุณจะได้เห็นว่าแต่ละคำสั่งทำงานอย่างไร และได้ลองเล่นกับผลลัพธ์แบบโต้ตอบได้ด้วย

### 3 ขั้นตอนหลักในการเรียนรู้ 🚀

คุณจะได้เรียนรู้ SQL แบบลงมือทำผ่านขั้นตอนเหล่านี้:

1.  **การตั้งค่าและเชื่อมต่อฐานข้อมูล** ⚙️
    -   ติดตั้งไลบรารี **R** ที่จำเป็น (`DBI`, `RSQLite`, `DT`).
    -   **ดาวน์โหลด** และ **สร้างการเชื่อมต่อ** ไปยังฐานข้อมูลตัวอย่าง **Chinook** เพื่อให้ R สามารถส่งคำสั่ง SQL ได้.
2.  **การสำรวจและรันคำสั่ง SQL สำคัญ** 💡
    -   ทำความเข้าใจและรันคำสั่งหลัก ๆ เช่น **JOIN**, **GROUP BY**, **HAVING**, **CASE WHEN**, และ **Common Table Expressions (CTEs)**.
    -   คิวรีเหล่านี้จะช่วยให้คุณสามารถรวมตาราง กรองข้อมูล และสร้างการวิเคราะห์เชิงลึกได้.
3.  **การวิเคราะห์ผลลัพธ์แบบโต้ตอบ** 📊
    -   **ตรวจสอบ** ผลลัพธ์ของคิวรีแต่ละข้อในรูปแบบ **ตารางแบบอินเทอร์แอคทีฟ** (Interactive Table) ด้วยแพ็คเกจ **DT**.
    -   คุณสามารถ **ค้นหา** และ **เรียงลำดับ** ข้อมูลได้ทันที ซึ่งช่วยให้เห็นภาพการทำงานของคำสั่ง SQL ได้อย่างชัดเจน.ย่างชัดเจน.

```{r setup, message=FALSE, warning=FALSE}
# Install packages if they are not already installed
if (!require(DBI)) install.packages("DBI")
if (!require(RSQLite)) install.packages("RSQLite")
if (!require(DT)) install.packages("DT") # For interactive tables

# Load libraries
library(DBI)
library(RSQLite)
library(DT)

# Download the Chinook database if it doesn't exist
db_file <- "chinook.db"
if (!file.exists(db_file)) {
  download.file("https://www.sqlitetutorial.net/wp-content/uploads/2018/03/chinook.zip", "chinook.zip")
  unzip("chinook.zip")
}

# Establish a connection to the database
con <- dbConnect(RSQLite::SQLite(), db_file)
```

## ![](DuckCool.png){width="22"} 2. JOIN Clause

The `JOIN` clause combines rows from multiple tables based on related columns. This query builds a complete music track overview by joining four tables: `Artists`, `Albums`, `Tracks`, and `Genres`.

มาเริ่มกันที่คำสั่ง `JOIN` ใช้สำหรับรวมแถวจากหลายตารางเข้าด้วยกัน โดยอ้างอิงจากคอลัมน์ที่มีความสัมพันธ์กัน. คำสั่งคิวรีนี้สร้างภาพรวมที่สมบูรณ์ของเพลง โดยการรวมสี่ตารางเข้าด้วยกัน ได้แก่: ศิลปิน (Artists), อัลบั้ม (Albums), เพลง (Tracks), และแนวเพลง (Genres)

```{sql connection=con, output.var="join_result"}
SELECT
    ar.Name AS ArtistName,
    al.Title as AlbumTitle,
    tr.Name AS TrackName,
    ge.Name AS Genre
FROM Artists ar
JOIN Albums  al ON ar.ArtistId = al.ArtistId
JOIN Tracks  tr ON al.AlbumId = tr.AlbumId
JOIN Genres  ge ON tr.GenreId = ge.GenreId
LIMIT 100;
```

> **Result:** Explore the first 100 tracks from the joined tables below. You can sort by any column or use the search box to filter.

```{r}
datatable(join_result, 
          options = list(pageLength = 5, autoWidth = TRUE),
          caption = "Interactive table of joined music data.",
          rownames = FALSE)
```

## ![](DuckCool.png){width="22"}3. Aggregate Functions

Aggregate functions like `COUNT()` summarize data across multiple rows. This query demonstrates how `COUNT(*)` (total rows) differs from `COUNT(column)` (total non-empty values in that column).

```{sql connection=con, output.var="count_result"}
SELECT
    COUNT(*) as TotalCustomers,
    COUNT(Company) as B2BCustomers
FROM Customers;
```

> **Result:** The query shows the total customer count versus the count of customers who have a company associated with them (B2B).

```{r}
datatable(count_result, 
          options = list(dom = 't'), ## 't' means table only, no search/paging
          caption = "Summary of total vs. B2B customers.",
          rownames = FALSE)
```

## ![](DuckCool.png){width="22"} 4. Advanced Aggregation (`GROUP BY`, `HAVING`)

Combine aggregates with `GROUP BY` and `HAVING` for powerful, filtered analysis. This query finds all non-USA countries with 5 or more customers and sorts them.

```{sql connection=con, output.var="filtered_agg_result"}
SELECT
    Country,
    COUNT(*) AS NumberOfCustomers
FROM Customers
WHERE Country <> 'USA'
GROUP BY Country
HAVING COUNT(*) >= 5
ORDER BY NumberOfCustomers DESC;
```

> **Result:** A ranked list of top customer countries (excluding the USA).

```{r}
datatable(filtered_agg_result,
          options = list(pageLength = 10),
          caption = "Countries (non-USA) with 5 or more customers.",
          rownames = FALSE)
```

## ![](DuckCool.png){width="22"} 5. Conditional Logic (`CASE WHEN`)

The `CASE` statement embeds if-then-else logic directly into your query. Here, we create a `Segment` column to classify customers as B2B (has a company) or B2C (no company).

```{sql connection=con, output.var="case_result"}
SELECT
    FirstName || ' ' || LastName AS FullName,
    Company,
    Country,
    CASE
        WHEN Company IS NULL THEN 'B2C (Consumer)'
        ELSE 'B2B (Business)'
    END AS Segment
FROM Customers
LIMIT 100;
```

> **Result:** The table below includes the dynamically generated `Segment` column. Try searching for "B2B" or "B2C".

```{r}
datatable(case_result,
          options = list(pageLength = 5),
          caption = "Customer segmentation using CASE WHEN.",
          rownames = FALSE)
```

## ![](DuckCool.png){width="22"}6. Common Table Expression (`WITH`)

Common Table Expressions (CTEs) make complex queries clean, readable, and modular by creating temporary, named result sets. Below, we compare a standard complex join with a much cleaner CTE-based approach to calculate total revenue from USA customers in 2009.

### Comparison: Standard Join vs. CTE {.tabset}

#### The Hard Way (Complex Join)

This single query combines filtering and joining in one block. It works, but it can be difficult to read and debug.

```{sql connection=con, eval=FALSE}
SELECT
    SUM(i.Total) as total_revenue_usa_cust_2009
FROM Customers c
JOIN Invoices i ON c.CustomerId = i.CustomerId
WHERE
    c.Country = 'USA' AND
    STRFTIME('%Y', i.InvoiceDate) = '2009';
```

#### The Better Way (with CTE)

This version breaks the problem down into logical steps. `usa_customers` and `invoices_y2009` are defined first, making the final `SELECT` statement simple and self-explanatory.

```{sql connection=con, output.var="cte_result"}
WITH usa_customers AS (
    SELECT * FROM Customers
    WHERE Country = 'USA'
), 
invoices_y2009 AS (
    SELECT * FROM Invoices
    WHERE STRFTIME('%Y', InvoiceDate) = '2009'
)
SELECT
    SUM(t2.Total) as TotalRevenueUsa2009
FROM usa_customers t1
JOIN invoices_y2009 t2
    ON t1.CustomerId = t2.CustomerId;
```

### Query Result

Both queries produce the same result, but the CTE approach is far more maintainable.

```{r}
# Format the result nicely
cte_result$TotalRevenueUsa2009 <- paste0("$", round(cte_result$TotalRevenueUsa2009, 2))

datatable(cte_result,
          options = list(dom = 't'),
          caption = "Total 2009 revenue from USA customers.",
          rownames = FALSE)
```

------------------------------------------------------------------------

```{r cleanup, include=FALSE}
# Close the database connection
dbDisconnect(con)
```

### 1. แนวคิดหลักทาง SQL ที่คุณจะได้พบ (SQL Concepts) 💡

เราจะเจาะลึกคำสั่ง SQL ที่สำคัญ โดยแบ่งออกเป็น 3 หมวดหมู่หลักตามขั้นตอนการประมวลผลของ Database Engine:

#### หมวดที่ 1: การเลือกและการกรองข้อมูล (Retrieval and Filtering)

| หัวข้อหลัก | Sub-Clause | หน้าที่โดยย่อ |
|:-----------------------|:-----------------------|:-----------------------|
| **โครงสร้างผลลัพธ์** | **`SELECT`** | กำหนด **คอลัมน์สุดท้าย** ที่ต้องการแสดงผล |
| **แหล่งข้อมูล** | **`FROM`** & **`JOIN`** | ระบุ **ตารางต้นทาง** และ **รวมตาราง** ที่เกี่ยวข้อง |
| **การกรองแถว** | **`WHERE`** | **กรองแถว** (Rows) ตามเงื่อนไขที่กำหนด **ก่อน** การจัดกลุ่ม |

------------------------------------------------------------------------

#### หมวดที่ 2: การจัดกลุ่มและการสรุปผล (Grouping and Summarization)

| หัวข้อหลัก | Sub-Clause | หน้าที่โดยย่อ |
|:-----------------------|:-----------------------|:-----------------------|
| **การจัดกลุ่ม** | **`GROUP BY`** | **จัดกลุ่ม** ข้อมูลเพื่อเตรียมการสรุปผล |
| **การคำนวณสรุป** | **AGGREGATE FUNCTION()** | ใช้ฟังก์ชันสรุปผล (เช่น `COUNT()`, `SUM()`, `AVG()`) เพื่อสร้างค่าเดียวต่อกลุ่ม |
| **การกรองกลุ่ม** | **`HAVING`** | **กรองกลุ่ม** ตามเงื่อนไขที่เกี่ยวข้องกับผลลัพธ์ของ Aggregate Function |

------------------------------------------------------------------------

#### หมวดที่ 3: การประยุกต์และเทคนิคขั้นสูง (Application and Advanced Techniques)

| หัวข้อหลัก | Sub-Clause | หน้าที่โดยย่อ |
|:-----------------------|:-----------------------|:-----------------------|
| **การจัดเรียง** | **`ORDER BY`** | **จัดเรียง** ผลลัพธ์สุดท้ายตามลำดับที่ต้องการ (`ASC` หรือ `DESC`) |
| **ตรรกะมีเงื่อนไข** | **`CASE WHEN`** | ฝังตรรกะ If-Then-Else เพื่อ **สร้างคอลัมน์ใหม่** หรือจำแนกประเภทข้อมูล |
| **โครงสร้างคิวรี** | **Common Table Expressions (CTEs)** | ใช้ **`WITH`** เพื่อกำหนดชุดผลลัพธ์ชั่วคราว ทำให้คิวรีที่ซับซ้อนดูสะอาดตาและเป็นระบบ |

------------------------------------------------------------------------

### 2. เครื่องมือการนำเสนอผลงาน (Portfolio Tools) 💻

เอกสารนี้ใช้พลังของ **R** เพื่อนำเสนอผลงานได้อย่างยอดเยี่ยม:

-   **โค้ด SQL ที่รันได้จริง** (Executable Code): คุณสามารถคัดลอกและนำไปใช้ต่อได้ทันที
-   **ผลลัพธ์แบบอินเทอร์แอคทีฟ** 📊: ผลลัพธ์จะแสดงในรูปแบบตารางที่สามารถ **ค้นหา** และ **จัดเรียง** ได้ด้วยแพ็คเกจ `DT`

**ลุยกัน เย้!**
